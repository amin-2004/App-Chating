class Message  {
    final String content;
  final bool isSentByUser; // لتحديد إذا كانت الرسالة مرسلة من المستخدم أم لا
  final DateTime timestamp;

  Message({
    required this.content,
    required this.isSentByUser,
    required this.timestamp,
  });





  }
  
